

  {
    "name": "st-agnes-register",
    "private": true,
    "engines": { "node": ">=22" },
    "scripts": {
      "build": "cd client && npm ci --include=dev && npm run build && cd ../server && npm ci --omit=dev",
      "start": "cd server && node src/index.js"
    }
  }